#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	    return (0);
}
int main(void)
{
	    char sentence[] = "Programming is like building a multilingual puzzle";

	        puts(sentence);
		    return (0);
}
